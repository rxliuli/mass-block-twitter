import { navigate, router, Route } from './route.svelte'
import Router from './Router.svelte'
import RouterView from './RouterView.svelte'

export { Router, RouterView, navigate, router }
export type { Route }
